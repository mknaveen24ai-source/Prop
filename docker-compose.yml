# ─────────────────────────────────────────────────────────────────────────────
# PropFirm Platform — Docker Compose
# Usage: docker compose up -d
# Prerequisites:
#   1. copy .env.template → .env and fill in all values
#   2. place fullchain.pem + privkey.pem in deploy/certs/
#   3. point MT5_BRIDGE_PATH at the host directory your MetaTrader 5 DWX
#      terminal writes into (see the price-feed note in DEPLOYMENT_CHECKLIST.md)
#
# The `version:` key is intentionally absent — it is obsolete under Compose v2
# and emits a deprecation warning.
# ─────────────────────────────────────────────────────────────────────────────

services:

  # ── PostgreSQL ───────────────────────────────────────────────────────────────
  postgres:
    # Major version and mount layout are a single compatibility contract. The
    # official PostgreSQL 18 image stores its cluster below
    # /var/lib/postgresql/18/docker; mounting only /var/lib/postgresql/data
    # makes an existing v18 volume look non-empty but uninitialised and the
    # container never becomes healthy. Never change this major version without
    # a verified dump/restore or pg_upgrade into a new volume.
    image: postgres:18-alpine
    container_name: propfirm_postgres
    restart: unless-stopped
    # The image default is max_connections=100, which the backend pool (60) plus
    # the migrate job, email worker and admin sessions can crowd. 200 leaves room
    # without the memory cost of going higher.
    command: >
      postgres
      -c max_connections=200
      -c shared_buffers=512MB
    environment:
      POSTGRES_DB:       ${DB_NAME:-propfirm}
      POSTGRES_USER:     ${DB_USER:-propfirm}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - pgdata:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-propfirm} -d ${DB_NAME:-propfirm}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - internal

  # ── Redis ────────────────────────────────────────────────────────────────────
  redis:
    image: redis:7-alpine
    container_name: propfirm_redis
    restart: unless-stopped
    command: redis-server --requirepass ${REDIS_PASSWORD:-redispass} --maxmemory 256mb --maxmemory-policy allkeys-lru
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD:-redispass}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - internal

  # ── PgBouncer (opt-in) ───────────────────────────────────────────────────────
  # Connection pooler in TRANSACTION mode. Lets several hundred client
  # connections share ~40 real Postgres backends, which is what makes running
  # more than one backend instance affordable.
  #
  # OPT-IN ON PURPOSE. Enable with:
  #     docker compose --profile pgbouncer up -d
  #     DB_POOLER_HOST=pgbouncer DB_POOLER_PORT=6432
  # Without those it does not start and every service connects straight to
  # Postgres, exactly as before. Changing the connection path of a live trading
  # system is not something to inherit silently from a git pull.
  #
  # Before enabling, know what transaction pooling does NOT preserve: session
  # state. Session-level advisory locks are the one thing in this codebase that
  # depends on it (utils/advisoryLock.js, guarding the scheduler jobs), which is
  # why the backend below is also given DIRECT_DATABASE_URL — that pool bypasses
  # the pooler. The pg_advisory_xact_lock calls used everywhere else are
  # transaction-scoped and safe through a pooler.
  pgbouncer:
    image: bitnami/pgbouncer:latest
    container_name: propfirm_pgbouncer
    restart: unless-stopped
    profiles: ["pgbouncer"]
    environment:
      PGBOUNCER_DATABASE:   ${DB_NAME:-propfirm}
      POSTGRESQL_HOST:      postgres
      POSTGRESQL_PORT:      5432
      POSTGRESQL_USERNAME:  ${DB_USER:-propfirm}
      POSTGRESQL_PASSWORD:  ${DB_PASSWORD}
      PGBOUNCER_POOL_MODE:  transaction
      PGBOUNCER_MAX_CLIENT_CONN:   500
      PGBOUNCER_DEFAULT_POOL_SIZE: 40
      PGBOUNCER_MIN_POOL_SIZE:     10
      # node-postgres sends statement_timeout as a startup parameter (see the
      # pool config in backend/db.js). PgBouncer rejects unknown startup
      # parameters in transaction mode unless they are listed here, so without
      # this every connection attempt fails immediately.
      PGBOUNCER_IGNORE_STARTUP_PARAMETERS: extra_float_digits,statement_timeout
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - internal

  # ── Migrations (one-shot) ────────────────────────────────────────────────────
  # Runs to completion, then exits. Backend waits on it via
  # `condition: service_completed_successfully`, so schema changes are applied
  # exactly once no matter how many backend replicas you run. Previously the
  # backend's own CMD ran `npm run migrate` on every container start, which
  # races itself the moment you scale past one.
  #
  # The command is `db:provision`, NOT `migrate`. `knex migrate:latest` cannot
  # provision a clean database and fails by design (see
  # backend/migrations/000_core_schema.js) — so on a fresh volume this service
  # exited non-zero and, because `backend` waits on it completing successfully,
  # the entire stack never started. db:provision baselines an empty database,
  # migrates an existing one, and verifies the result either way.
  migrate:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: propfirm_migrate
    restart: "no"
    env_file: .env
    environment:
      NODE_ENV:     production
      # Always straight to Postgres, never through PgBouncer: DDL and the
      # migration advisory lock are session-scoped and do not survive
      # transaction pooling.
      DATABASE_URL: postgresql://${DB_USER:-propfirm}:${DB_PASSWORD}@postgres:5432/${DB_NAME:-propfirm}
    entrypoint: ["dumb-init", "--"]
    command: ["npm", "run", "db:provision"]
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - internal

  # ── Backend API ──────────────────────────────────────────────────────────────
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    # container_name is deliberately absent: Docker refuses duplicate
    # names, so setting it makes `docker compose up --scale` fail outright.
    # (audit finding H-08 — this service is horizontally scalable.)
    # The all-in-one deployment. It is deliberately mutually exclusive with
    # the scaleout profile: running both would create two trade engines.
    profiles: ["monolith"]
    restart: unless-stopped
    env_file: .env
    environment:
      NODE_ENV:     production
      TRUST_PROXY:  ${TRUST_PROXY:-1}
      # Defaults to postgres:5432. Set DB_POOLER_HOST=pgbouncer and
      # DB_POOLER_PORT=6432 (with --profile pgbouncer) to route through the
      # pooler; nothing else needs to change.
      DATABASE_URL: postgresql://${DB_USER:-propfirm}:${DB_PASSWORD}@${DB_POOLER_HOST:-postgres}:${DB_POOLER_PORT:-5432}/${DB_NAME:-propfirm}
      # Session-scoped advisory locks (the scheduler mutex) must bypass any
      # pooler. See the directPool comment in backend/db.js.
      DIRECT_DATABASE_URL: postgresql://${DB_USER:-propfirm}:${DB_PASSWORD}@postgres:5432/${DB_NAME:-propfirm}
      # Optional read replica for analytics, transparency and the leaderboard.
      # Falls back to DATABASE_URL when unset.
      READ_DATABASE_URL: ${READ_DATABASE_URL:-}
      REDIS_URL:    redis://:${REDIS_PASSWORD:-redispass}@redis:6379
      # Trade engine mode. `event` (default since 2026-08-17) drives SL/TP,
      # pending fills, drawdown and profit targets off the price tick itself,
      # demoting the polling loops to safety fallbacks. `interval` keeps the
      # original 500ms/1s polling cadence. Both make identical decisions —
      # verified by scripts/engine-equivalence.js. Flip back to `interval` and
      # restart to roll back; no migration needs reverting. The default here
      # must match .env.template, which documents the decision in full.
      ENGINE_MODE:  ${ENGINE_MODE:-event}
      # Where the MT5/DWX bridge files are mounted inside the container.
      DWX_PATH:     /app/mt5-bridge
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    volumes:
      - uploads:/app/uploads
      - logs:/app/logs
      # ── MT5 price-feed bridge ────────────────────────────────────────────────
      # The price feed is FILE-BASED ONLY (priceFeed.js reads DWX_Market_Data.txt
      # etc.; price_feed_sources.source_type accepts only 'shared_env' and
      # 'dwx_path'). There is no HTTP/WebSocket market-data provider, and this
      # alpine container has no MetaTrader. A MetaTrader 5 terminal must run
      # OUTSIDE these containers — a Windows host or Wine — writing its DWX
      # files into MT5_BRIDGE_PATH on the host.
      #
      # Read-write: the backend also writes DWX_Commands_*.txt to subscribe to
      # symbols. Set MT5_BRIDGE_PATH in .env.
      - ${MT5_BRIDGE_PATH:-./deploy/mt5-bridge}:/app/mt5-bridge
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:5000/api/health >/dev/null 2>&1 || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5
    networks:
      - internal
      - external

  # ── Frontend ─────────────────────────────────────────────────────────────────
  email-worker:
    build:
      context: .
      dockerfile: backend/Dockerfile
    # container_name is deliberately absent: Docker refuses duplicate
    # names, so setting it makes `docker compose up --scale` fail outright.
    # (audit finding H-08 — this service is horizontally scalable.)
    restart: unless-stopped
    env_file: .env
    environment:
      NODE_ENV: production
      # Short transactions only, so it is safe through the pooler when enabled.
      DATABASE_URL: postgresql://${DB_USER:-propfirm}:${DB_PASSWORD}@${DB_POOLER_HOST:-postgres}:${DB_POOLER_PORT:-5432}/${DB_NAME:-propfirm}
      DIRECT_DATABASE_URL: postgresql://${DB_USER:-propfirm}:${DB_PASSWORD}@postgres:5432/${DB_NAME:-propfirm}
      EMAIL_WORKER_POLL_MS: ${EMAIL_WORKER_POLL_MS:-5000}
      EMAIL_WORKER_BATCH_SIZE: ${EMAIL_WORKER_BATCH_SIZE:-10}
    command: ["npm", "run", "email:worker"]
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - logs:/app/logs
    networks:
      - internal

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
      args:
        # MUST stay empty. services/api.js uses VITE_API_URL as the axios
        # baseURL, and every call path already starts with /api/... — so behind
        # the same-origin nginx proxy an empty baseURL resolves correctly to
        # /api/...  Setting it to a full URL produces /api/api/... and breaks
        # every request. Only override this if the API is on another origin.
        VITE_API_URL:     "${VITE_API_URL-}"
        VITE_APP_VERSION: ${APP_VERSION:-1.0.0}
        VITE_SENTRY_DSN:  ${FRONTEND_SENTRY_DSN:-}
        # Fed from the backend's own flag, not a separate variable, so the two
        # cannot be set to different values. Vite inlines this at BUILD time,
        # so flipping FX_CONVERSION_ENABLED needs `docker compose up -d --build
        # frontend` — restarting the container alone will not pick it up.
        VITE_FX_CONVERSION_ENABLED: ${FX_CONVERSION_ENABLED:-false}
    # container_name is deliberately absent: Docker refuses duplicate
    # names, so setting it makes `docker compose up --scale` fail outright.
    # (audit finding H-08 — this service is horizontally scalable.)
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1/ >/dev/null 2>&1 || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5
    networks:
      - external

  # ── Demo price feed (opt-in) ─────────────────────────────────────────────────
  # The real feed is file-based: a MetaTrader 5 terminal outside these containers
  # writes DWX_Market_Data.txt into MT5_BRIDGE_PATH. Until one is attached there
  # are no prices, so nothing on the platform can be exercised end-to-end — which
  # makes a first deploy impossible to verify.
  #
  # This writes the same file with SYNTHETIC prices, into the same bridge
  # directory the backend reads. Start it with:
  #
  #     docker compose --profile demo up -d      (or: ./deploy.sh --demo)
  #
  # OPT-IN, and the script itself refuses to run under NODE_ENV=production
  # unless ALLOW_DEMO_FEED=true is set explicitly — random walks pricing real
  # trades is the one accident this must never have. Stop it before attaching a
  # real terminal, or both will write the same file:
  #
  #     docker compose --profile demo stop dwx-demo
  dwx-demo:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: propfirm_dwx_demo
    profiles: ["demo"]
    restart: unless-stopped
    environment:
      # Deliberately NOT `env_file: .env` — that would set NODE_ENV=production
      # and trip the guard for the demo case this service exists to serve. The
      # override stays an explicit choice made in .env, not one inherited here.
      NODE_ENV: development
      DWX_MARKET_DATA_FILE: /app/mt5-bridge/DWX_Market_Data.txt
    entrypoint: ["dumb-init", "--"]
    command: ["node", "--enable-source-maps", "dist/scripts/fake-dwx-feed.js", "--hz=5", "--symbols=6"]
    volumes:
      - ${MT5_BRIDGE_PATH:-./deploy/mt5-bridge}:/app/mt5-bridge
    networks:
      - internal

  # ═══════════════════════════════════════════════════════════════════════════
  # SPLIT ROLES (opt-in) — the topology for 10K+ concurrent traders
  # ═══════════════════════════════════════════════════════════════════════════
  # Start with:
  #     docker compose --profile scaleout up -d --scale gateway=3 --scale api=2
  # The profile starts nginx-scaleout with the matching vhost automatically.
  #
  # OPT-IN, like pgbouncer above. Without the profile none of this starts and
  # `backend` (ROLE unset → 'all') runs exactly as it always has.
  #
  # ── Why the split ──
  #
  # The trade index (backend/utils/tradeIndex.js) is in process memory and is
  # read synchronously inside the tick's scan loop. That is why a 100K-trade tick
  # costs ~11.5ms, and it is also why there can only ever be ONE engine.
  #
  # One process cannot also serve 10,000 websockets: the fan-out is bytes-out
  # linear in connections. So the engine and the sockets go in different
  # processes, and only the socket tier is scaled.
  #
  # DO NOT scale `engine`. A second one would build its own index, hold a stale
  # view of trades the first opened and closed, and disagree about floating PnL.
  # Closes stay correct either way (FOR UPDATE SKIP LOCKED) and the sweeps are
  # advisory-locked — but nothing would tell you the index had drifted.

  engine:
    build:
      context: .
      dockerfile: backend/Dockerfile
    profiles: ["scaleout"]
    container_name: propfirm_engine   # exactly one — naming it makes that literal
    restart: unless-stopped
    env_file: .env
    environment:
      NODE_ENV:     production
      ROLE:         engine
      TRUST_PROXY:  ${TRUST_PROXY:-1}
      # 100K trade entries plus ~30K account entries is 70-100MB of live objects
      # before V8's own overhead. The default heap is enough today and would not
      # be at target, and the failure mode is an OOM kill of the one process that
      # closes stop-losses.
      NODE_OPTIONS: ${ENGINE_NODE_OPTIONS:---max-old-space-size=4096}
      DATABASE_URL: postgresql://${DB_USER:-propfirm}:${DB_PASSWORD}@${DB_POOLER_HOST:-postgres}:${DB_POOLER_PORT:-5432}/${DB_NAME:-propfirm}
      DIRECT_DATABASE_URL: postgresql://${DB_USER:-propfirm}:${DB_PASSWORD}@postgres:5432/${DB_NAME:-propfirm}
      READ_DATABASE_URL: ${READ_DATABASE_URL:-}
      REDIS_URL:    redis://:${REDIS_PASSWORD:-redispass}@redis:6379
      ENGINE_MODE:  ${ENGINE_MODE:-interval}
      DWX_PATH:     /app/mt5-bridge
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    volumes:
      - uploads:/app/uploads
      - logs:/app/logs
      - ${MT5_BRIDGE_PATH:-./deploy/mt5-bridge}:/app/mt5-bridge
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:5000/api/health >/dev/null 2>&1 || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5
    networks:
      - internal
      - external

  # Socket tier. This is the service you add replicas of when more traders log
  # in. No index, no schedulers, no price feed — it receives price deltas and
  # batched equity over Redis and writes them to its own connections.
  gateway:
    build:
      context: .
      dockerfile: backend/Dockerfile
    profiles: ["scaleout"]
    restart: unless-stopped
    env_file: .env
    environment:
      NODE_ENV:     production
      ROLE:         gateway
      TRUST_PROXY:  ${TRUST_PROXY:-1}
      DATABASE_URL: postgresql://${DB_USER:-propfirm}:${DB_PASSWORD}@${DB_POOLER_HOST:-postgres}:${DB_POOLER_PORT:-5432}/${DB_NAME:-propfirm}
      DIRECT_DATABASE_URL: postgresql://${DB_USER:-propfirm}:${DB_PASSWORD}@postgres:5432/${DB_NAME:-propfirm}
      # Redis is not optional here: it is the only path from the engine to these
      # traders. Without it a gateway accepts connections and sends nothing.
      REDIS_URL:    redis://:${REDIS_PASSWORD:-redispass}@redis:6379
    depends_on:
      redis:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    # Each socket is a file descriptor, and the container limit is reached long
    # before the process is busy.
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:5000/api/health >/dev/null 2>&1 || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5
    networks:
      - internal
      - external

  # HTTP tier. Stateless — scale it on CPU like any other web tier.
  api:
    build:
      context: .
      dockerfile: backend/Dockerfile
    profiles: ["scaleout"]
    restart: unless-stopped
    env_file: .env
    environment:
      NODE_ENV:     production
      ROLE:         api
      TRUST_PROXY:  ${TRUST_PROXY:-1}
      DATABASE_URL: postgresql://${DB_USER:-propfirm}:${DB_PASSWORD}@${DB_POOLER_HOST:-postgres}:${DB_POOLER_PORT:-5432}/${DB_NAME:-propfirm}
      DIRECT_DATABASE_URL: postgresql://${DB_USER:-propfirm}:${DB_PASSWORD}@postgres:5432/${DB_NAME:-propfirm}
      READ_DATABASE_URL: ${READ_DATABASE_URL:-}
      REDIS_URL:    redis://:${REDIS_PASSWORD:-redispass}@redis:6379
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    volumes:
      - uploads:/app/uploads
      - logs:/app/logs
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:5000/api/health >/dev/null 2>&1 || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5
    networks:
      - internal
      - external

  # ── Nginx Reverse Proxy ───────────────────────────────────────────────────────
  nginx:
    image: nginx:1.25-alpine
    container_name: propfirm_nginx
    profiles: ["monolith"]
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      # Main config. Exists for worker_connections, which cannot be set from
      # conf.d and whose stock value (1024, x2 per proxied WebSocket) caps the
      # proxy at roughly 2,000 concurrent traders regardless of hardware.
      #
      # Running split roles? Swap the vhost below for propfirm.scaleout.conf,
      # which routes sockets and API traffic to separate upstreams and makes
      # socket sessions sticky.
      - ./deploy/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./deploy/nginx/propfirm.conf:/etc/nginx/conf.d/default.conf:ro
      - ./deploy/certs:/etc/nginx/certs:ro
      - nginx_logs:/var/log/nginx
    # worker_rlimit_nofile is a request nginx cannot grant itself: without a
    # matching container limit, raising worker_connections just moves the failure
    # from "worker_connections are not enough" to "Too many open files".
    ulimits:
      nofile:
        soft: 131072
        hard: 131072
    depends_on:
      backend:
        condition: service_healthy
      frontend:
        condition: service_healthy
    networks:
      - external

  # Public proxy for the split-role topology. This is a separate service rather
  # than an override of `nginx` so Compose cannot accidentally retain the
  # monolith's backend dependency or vhost when --profile scaleout is selected.
  nginx-scaleout:
    image: nginx:1.25-alpine
    container_name: propfirm_nginx_scaleout
    profiles: ["scaleout"]
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./deploy/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./deploy/nginx/propfirm.scaleout.conf:/etc/nginx/conf.d/default.conf:ro
      - ./deploy/certs:/etc/nginx/certs:ro
      - nginx_logs:/var/log/nginx
    ulimits:
      nofile:
        soft: 131072
        hard: 131072
    depends_on:
      gateway:
        condition: service_healthy
      api:
        condition: service_healthy
      frontend:
        condition: service_healthy
    networks:
      - external

volumes:
  pgdata:
  redisdata:
  uploads:
  logs:
  nginx_logs:

networks:
  internal:
    driver: bridge
    internal: true   # postgres + redis not reachable from outside
  external:
    driver: bridge
